language: java
jdk:
- oraclejdk8
node_js:
- '7'
services:
- mysql
- mongodb
cache:
  yarn: true
  directories:
  - node_modules
before_install:
- pip install --user codecov
- nvm install 7
- npm install -g yarn
- yarn install
install:
- cd mr-gui && yarn install && cd ..
- mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
before_script:
- mysql -e "create database IF NOT EXISTS sr; grant all on sr.* to 'root';SET
  GLOBAL TRANSACTION ISOLATION LEVEL SERIALIZABLE;" -uroot
script:
- mvn test
- cd mr-gui && yarn install
after_success:
- codecov