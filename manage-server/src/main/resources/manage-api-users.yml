# Valid scopes are manage.api.Scope.values(); =>
apiUsers:
  - {
    name: "dashboard",
    password: "secret",
    scopes: [ READ, CHANGE_REQUEST_SP, CHANGE_REQUEST_IDP, TEST, POLICIES ]
  }
  - {
    name: "pdp",
    password: "secret",
    scopes: [READ]
  }
  - {
    name: "stats",
    password: "secret",
    scopes: [READ]
  }
  - {
    name: "sram",
    password: "secret",
    scopes: ["READ", "WRITE_SP", "DELETE_SP"]
  }
  - {
    name: "sp-portal",
    password: "secret",
    scopes: [ READ, WRITE_SP, DELETE_SP, PUSH, CHANGE_REQUEST_SP, TEST ]
  }
  - {
    name: "sysadmin",
    password: "secret",
    scopes: [ READ, WRITE_IDP, WRITE_SP, PUSH, SYSTEM ]
  }
