# Valid scopes are manage.api.Scope.values(); =>  ["READ", "WRITE", "PUSH", "SYSTEM", "TEST"]
apiUsers:
  - {
    name: "dashboard",
    password: "secret",
    scopes: [READ, CHANGE_REQUEST, TEST]
  }
  - {
    name: "pdp",
    password: "secret",
    scopes: [READ]
  }
  - {
    name: "stats",
    password: "secret",
    scopes: [READ]
  }
  - {
    name: "sp-portal",
    password: "secret",
    scopes: [READ, WRITE, PUSH, TEST]
  }
  - {
    name: "sysadmin",
    password: "secret",
    scopes: [READ, WRITE, PUSH, SYSTEM]
  }
